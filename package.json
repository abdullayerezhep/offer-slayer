{
  "name": "offer-slayer",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "b": "pnpm --filter @offer-slayer/backend",
    "f": "pnpm --filter @offer-slayer/frontend",
    "dev": "pnpm -r --parallel dev",
    "types": "pnpm -r --parallel types",
    "prettify": "pnpm -r --parallel prettify",
    "lint": "pnpm -r -parallel lint"
  },
  "devDependencies": {
    "eslint": "^9.17.0",
    "eslint-config-prettier": "^10.0.1",
    "eslint-config-react-app": "^7.0.1",
    "eslint-config-standard-with-typescript": "^43.0.1",
    "prettier": "^3.4.2",
    "rimraf": "^6.0.1",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.7.3"
  }
}
